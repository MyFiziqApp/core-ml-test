<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/container"
    style="@style/MFViewAvatarItemLoadingLayout">

    <ImageView
        android:id="@+id/loadingMan"
        style="@style/MFViewAvatarLoadingMan"
        android:src="@drawable/loading_man"
        app:layout_constraintBottom_toTopOf="@+id/title"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/loadingIcon"
        style="@style/MFViewAvatarLoadingIcon"
        android:src="@drawable/ic_loader"
        app:layout_constraintBottom_toBottomOf="@+id/loadingMan"
        app:layout_constraintLeft_toLeftOf="@+id/loadingMan"
        app:layout_constraintRight_toRightOf="@+id/loadingMan"
        app:layout_constraintTop_toTopOf="@+id/loadingMan" />

    <TextView
        android:id="@+id/title"
        style="@style/MFViewAvatarLoadingText"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/loadingMan" />

</androidx.constraintlayout.widget.ConstraintLayout>
