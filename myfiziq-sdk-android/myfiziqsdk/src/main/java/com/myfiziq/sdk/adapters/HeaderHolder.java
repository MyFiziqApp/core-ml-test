package com.myfiziq.sdk.adapters;

/**
 * A Holder for a Header item.
 */
class HeaderHolder
{
    final int mId;
    final HolderViewBinder mBinder;

    public HeaderHolder(int id, HolderViewBinder binder)
    {
        mId = id;
        mBinder = binder;
    }
}
