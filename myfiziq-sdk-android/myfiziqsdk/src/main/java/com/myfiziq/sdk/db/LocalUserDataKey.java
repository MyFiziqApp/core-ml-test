package com.myfiziq.sdk.db;

/**
 * Contains a list of keys that can be stored in {@link ModelLocalUserData} to persist user
 * information locally on the device.
 */
public enum LocalUserDataKey
{
    DATE_OF_BIRTH,
    ETHNICITY
}
